{% extends 'base.html.twig' %}

{% block title %}
{% endblock %}

{% block body %}
    <h1>Ville</h1>
    <div>
        <table class="table table-striped table-dark">
            <thead>
            <tr>
                <th scope="col">Nom</th>
                <th scope="col">codePostal</th>
                <th scope="col">nbLieux enregistré</th>
            </tr>
            </thead>
            <tbody>
            {% for ville in villes %}
            <tr>
                <td>{{ ville.nom }}</td>
                <td>{{ ville.codePostal }}</td>
                <td>{{ ville.lieus.count }}</td>
                <td>
                    <a href="#"><input type="button"value="Supprimer ville"></a>
                </td>
            </tr>
            </tbody>
            {% endfor %}
        </table>
        <form action="{{ path('admin_ville_add') }}">
            <input type="submit" class="btn btn-primary" value="Ajouter une ville">
        </form>
    </div>
{% endblock %}

